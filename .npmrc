# React Native/Expo projects often have transient peer dependency conflicts
# due to web dependencies (react-dom) that don't affect native mobile builds.
# This setting allows npm to resolve these gracefully.
legacy-peer-deps=true
